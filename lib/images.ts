/* ============================================================
   Shared image paths. Drop the matching PNG into /public/images/…
   and it activates automatically (see public/images/README.md).
   Components use <SmartImage>, which falls back gracefully when a
   file is not present yet.
   ============================================================ */

export const heroImage = "/images/hero/florida-waterfront.png";

/** Primary office photo (Contact + About). Name your file office-1.png. */
export const officeImage = "/images/office/office-1.png";

/** Optional decorative background band. */
export const aboutImage = "/images/about/firm-1.png";
